// Generated by LispyScript v1.0.0
'use strict';
require("angular");
var part = require("/home/aaron/node_modules/lib/lib/functional/partialApplication.js");
var curry = part["curry"];
var defer = part["defer"];
var midi = require("./midi.js");
var app = angular.module("pianojs",[]);
var evented = require("/home/aaron/node_modules/lib/lib/async/eventedInterface.js");
(function() {
    function midiDevices($q) {
        console.log("midiDevices");
        var inputs = null;
        var outputs = null;
        var midiAccess = null;
        var objInter = null;
        ($q.resolve(midi())).then(function(access) {
            midiAccess = midiAccess;
            inputs = access.inputs;
            outputs = access.outputs;
            return access;
        });
        objInter = {
            on: function(event,handler) {
                access.addEventListener(event,handler,false);
                return this;
            },
            getInputs: function() {
                return inputs;
            },
            getOutputs: function() {
                return outputs;
            },
            getAccess: function() {
                return midiAccess;
            }
        };
    }
    return app.factory(midiDevices["name"],midiDevices);
})();
(function() {
    function modelFiles() {
        return console.log("model files");
    }
    return app.factory(modelFiles["name"],modelFiles);
})();
(function() {
    function devicesController($scope) {
        return console.log("devices controller");
    }
    return app.controller(devicesController["name"],devicesController);
})();
(function() {
    function fileController($scope) {
        return console.log("file controller");
    }
    return app.controller(fileController["name"],fileController);
})();
(function() {
    function playerController($scope,midiDevices) {
        return console.log("player controller");
    }
    return app.controller(playerController["name"],playerController);
})();
(function() {
    function modelController($scope) {
        return console.log("model controller");
    }
    return app.controller(modelController["name"],modelController);
})();
