"use strict";

var DLists      = require( "./DoublyLinkedList.js" );
var F           = require( "./functional.js" );
global.Promise  = require( "bluebird" );
global.F        = F;
global.curry    = F.curry;

//if we get more math functions, need a math lib file
Math.randomInt = function getRandomInt( min, max )
{
	return Math.floor( Math.random() * ( max - min + 1 ) ) + min;
};

global.fs = require( "fs" );
Promise.promisifyAll( fs );
global.obj = require( "./object" );
global.Err = require( "./errors.js" );
global.Identity = global.Id = global.I = ( ( a ) => a );

var Globals = module.exports = {
    defer      : F.defer,
	genErr     : Err.gen,
	arr        : require( "./array.js" ),
	obj        : require( "./object.js" ),
	DoublyLinkedList: DLists.DoublyLinkedList,
	prom       : require( "./promise.js" ),
	is         : F.is,
	curry      : F.curry,
	cond       : F.cond,
	maybe      : F.maybe,
};
