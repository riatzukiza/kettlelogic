var Interface = require("lib/lib/interface").Interface;

var StateChain = Interface
        .Constructor(function StateChain(scope) {
            this.init(scope)
        })
        .methods({
            init() {
            },
            end() {
                
            },
        })
